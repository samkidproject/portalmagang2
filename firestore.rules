rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Catch-all rule to guarantee full read/write permissions for all collections and subcollections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
